# URL base del backend (solo necesaria si no usás el proxy de Vite)
# En desarrollo el proxy de vite.config.ts redirige /convocatorias y /propuestas automáticamente
# VITE_API_URL=http://localhost:3000

# En producción descomentar y apuntar al servidor:
# VITE_API_URL=https://api.becas.ejemplo.edu.ar

